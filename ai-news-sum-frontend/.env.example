# Frontend Environment Variables (Next.js)
# ----------------------------------------
# NOTE: Only variables with the NEXT_PUBLIC_ prefix are exposed to the browser.

# API endpoint that your frontend communicates with (replace with your actual API URL)
NEXT_PUBLIC_API_URL=https://api.example.com

# Add any other public environment variables needed by your frontend
# e.g., NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id